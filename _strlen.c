#include "main.h"

/**
 * _strlen - Returns the lenght of a string.
 *
 * @s: Type char pointer
 *
 * Return: Always 0.
 *
 */

int _strlen(const char *s)
{
	int len;

	for (len = 0; s[len] != 0; len++)
	{
	}
	return (len);
}
